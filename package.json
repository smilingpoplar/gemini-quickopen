{
  "name": "gemini-url-quickopen",
  "version": "1.0.0",
  "description": "点击图标即可将当前网页 URL 发送到 Gemini 进行分析。",
  "scripts": {
    "dev": "extension dev --polyfill",
    "start": "extension start --polyfill",
    "preview": "extension preview --polyfill",
    "build": "npm run build:chrome && npm run build:firefox",
    "build:chrome": "extension build --browser=chrome --polyfill --zip",
    "build:firefox": "extension build --browser=firefox --polyfill --zip",
    "test": "node --test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "canvas": "^3.2.1",
    "extension": "^3.5.1"
  },
  "dependencies": {
    "webext-patterns": "^1.5.1"
  }
}
